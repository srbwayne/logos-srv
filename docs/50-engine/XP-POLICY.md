# XP Policy

The XP policy must make explicit:

- base XP;
- factor matching and cutoff semantics;
- multipliers;
- percentage weights;
- composition of multiple factors;
- skill bonuses;
- integer conversion/rounding.

Current behavior is the baseline until tests prove and a decision changes it.
