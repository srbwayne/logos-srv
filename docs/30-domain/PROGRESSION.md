# Progression

Progression is the future central domain capability. It should accept explicit inputs and return an inspectable result rather than hiding arithmetic inside orchestration.

Target conceptual result:

```text
ProgressionResult
├── globalXpDelta
├── globalLevelTransition
├── attributeDeltas
├── skillEffects
├── stressDelta
└── appliedRules / explanation
```

Exact fields and persistence semantics require characterization of existing behavior before contract freeze.
