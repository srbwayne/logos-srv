# Agent Tasks

Task lifecycle:

```text
backlog -> active -> done
```

Each task should contain objective, allowed scope, forbidden scope, required reading, implementation steps, validation commands, stop conditions and final-report format.

Agents must work on one migration slice at a time.
